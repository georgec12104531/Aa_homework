
// madLib
function madLib(verb, adj, noun) {
  return `I want to ${verb} ${adj} ${noun} right now!`
}

// isSubString

function isSubString(string, substring) {
  if (string.includes(substring)) {
    return true;
  }
  return false;
}

// fizzbuzz


function fizzBuzz()
